# SureBet Pro – Production Architecture

This directory is reserved for the modular prediction-engine migration.

Current production entrypoint remains `server.js` while the legacy application is stabilized incrementally.
