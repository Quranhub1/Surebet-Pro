{
  "name": "surebet-pro-orchestrator",
  "version": "2.1.0",
  "description": "Multi-model football forecasting, automated daily generation and result tracking engine",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check server.js && node --check src/server-v2.js && node --check src/storage.js && node --check src/prediction-engine.js && node --check scripts/automation-trigger.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "axios": "^1.6.2",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "groq-sdk": "^0.12.0",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
